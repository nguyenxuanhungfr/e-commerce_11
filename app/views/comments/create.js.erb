<% if @status == false %>
  toastr.error('<%= t "comments_error" %>')
<% else %>
  toastr.success('<%= t "comments" %>')
  $('.user_comment').html('<%= j(render partial: "comments/comment",collection: @support.comments, as: :comment) %>')
  $('#comment_description').val('')
<% end %>
